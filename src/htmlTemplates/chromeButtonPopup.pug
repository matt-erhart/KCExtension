doctype html

html
  head
    meta(charset='UTF-8')
    title Set your current goal.
    style.
      body { width: 500px; }

  body
    #root
    if env !== 'prod'
    script(src=env == 'prod' ? '/js/chromeButtonPopup.bundle.js' : 'http://localhost:3000/js/chromeButtonPopup.bundle.js')
