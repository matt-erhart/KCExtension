doctype html

html
  head
    meta(charset='UTF-8')
    title Snippet: Crop then comment.
    style.
      body { width: 500px; }

  body
    #root
    if env !== 'prod'
    script(src=env == 'prod' ? '/js/app.bundle.js' : 'http://localhost:3000/js/app.bundle.js')
